import { FormatMoedaPipe } from './format-moeda.pipe';

describe('FormatMoedaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMoedaPipe();
    expect(pipe).toBeTruthy();
  });
});
